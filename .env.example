# =============================================================================
# x402-api Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values

# =============================================================================
# Cloudflare Worker Configuration
# =============================================================================

# Environment name (staging or production)
ENVIRONMENT=staging

# =============================================================================
# x402 Protocol Configuration
# =============================================================================

# Stacks address to receive payments
X402_SERVER_ADDRESS=ST...

# Network: mainnet or testnet
X402_NETWORK=testnet

# x402 facilitator URL
X402_FACILITATOR_URL=https://stacksx402.com

# =============================================================================
# API Keys (set via wrangler secret put for deployed workers)
# =============================================================================

# OpenRouter API key for LLM inference
OPENROUTER_API_KEY=sk-or-...

# Hiro API key for better rate limits (optional but recommended)
HIRO_API_KEY=...

# =============================================================================
# Testing Configuration
# =============================================================================

# Mnemonic for test wallet (TESTNET ONLY - never use mainnet funds!)
# Used by: npm run test, npm run test:full, etc.
X402_CLIENT_PK=your twelve word testnet mnemonic phrase here

# Worker URL for testing (defaults to localhost:8787)
# For staging tests: https://x402.aibtc.dev
# For production tests: https://x402.aibtc.com
X402_WORKER_URL=http://localhost:8787

# Enable verbose test output (1 = enabled)
VERBOSE=0

# Delay between tests in milliseconds
TEST_DELAY_MS=500

# Max retries for rate-limited requests
TEST_MAX_RETRIES=3
