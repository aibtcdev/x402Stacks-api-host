# =============================================================================
# x402 API Environment Variables
# =============================================================================
# Copy to .env for local development
# For Cloudflare deployment, use `wrangler secret put` for secrets

# Cloudflare API credentials (for CI/CD)
# Create token at https://dash.cloudflare.com/profile/api-tokens
# Use "Edit Cloudflare Workers" template
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# =============================================================================
# Secrets (set via `wrangler secret put <NAME>`)
# =============================================================================

# OpenRouter API key for LLM inference
# Get one at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Hiro API key for better rate limits (optional but recommended)
# Get one at: https://platform.hiro.so/
HIRO_API_KEY=

# =============================================================================
# Environment Variables (configured in wrangler.jsonc per environment)
# =============================================================================

# ENVIRONMENT: development | staging | production
# X402_FACILITATOR_URL: https://facilitator.x402stacks.xyz
# X402_NETWORK: mainnet | testnet
# X402_SERVER_ADDRESS: Your Stacks address to receive payments

# =============================================================================
# KV Namespaces (create via wrangler, update IDs in wrangler.jsonc)
# =============================================================================

# wrangler kv:namespace create METRICS
# wrangler kv:namespace create STORAGE
